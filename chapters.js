// 單元清單：每個單元對應書本一個完整單元（例如 Ch01 單元1-1），檔案裡放該單元目前
// 已經 OCR 進來的全部單字。之後同一單元有新照片，直接把新單字加進對應的 json 檔案即可，
// 不用開新檔案。
//
// 「每天10個字」的教學批次是 app.js 在載入時，把所有單元的單字依序串成一條連續清單後，
// 每10個字自動切一批（batch）算出來的，不是照單元邊界切——如果某單元最後一天不滿10個字，
// 會直接跨到下一個單元的開頭補滿，確保每天都剛好10個字（最後一批如果全書字數不到10個字，
// 才會是不滿10個字的批次）。
const UNITS = [
  { id: "ch01-1", name: "Ch01 日常生活 單元1-1", file: "data/ch01-1.json" },
  { id: "ch01-2", name: "Ch01 日常生活 單元1-2", file: "data/ch01-2.json" }
];
